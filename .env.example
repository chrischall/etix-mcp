# No credentials needed. Etix event/venue discovery is public; requests are
# routed through your signed-in etix.com browser tab via the fetchproxy
# extension (which clears the DataDome interstitial). This file is a marker only.
#
# Optional overrides:
# ETIX_WS_PORT=37149     # fetchproxy WebSocket port (shared fleet port)
# ETIX_DEBUG=1           # stderr per-request bridge timing
